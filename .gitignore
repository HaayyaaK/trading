# OS / editor
desktop.ini
Thumbs.db
.vscode/
*.swp

# Node / Python tooling
node_modules/
__pycache__/
*.pyc

# generated artifacts (rebuilt by tests / tools)
test/mine.json
tools/seeds.json

# stray pre-existing file (inert duplicate of this ignore file, kept on disk)
gitignore

# Local vendor libraries live in assets/ but are large source trees; Track 4
# will commit only the specific dist files it wires in, not the whole trees.
assets/Chart.js-v4.5.1/
assets/echarts-v6.1.0/
assets/fontawesome-free-v7.3.1-web/
